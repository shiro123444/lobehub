'use client';

import SubmissionCenter from '@/features/NexusRegistry/SubmissionCenter';

const CommunityReviewPage = () => {
  return <SubmissionCenter mode="review" />;
};

export default CommunityReviewPage;
